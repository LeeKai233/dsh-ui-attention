/** Browser half of the dsh-ui-attention plugin (placeholder during scaffold; T11 fills it in). */

export const inject: string[] = []

/** @param ctx - client cordis context. */
export function apply(ctx: unknown): void {
  void ctx
}
