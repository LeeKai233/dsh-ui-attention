/** Node half of the dsh-ui-attention plugin (placeholder during scaffold; T10 fills it in). */

export const inject: string[] = []

/** @param ctx - host cordis context. */
export function apply(ctx: unknown): void {
  void ctx
}
